{
  "name": "counttoamillion-bot",
  "version": "1.0.0",
  "description": "A Slack bot for the #counttoamillion channel that validates counts, tracks progress, and enforces counting rules.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest"
  },
  "dependencies": {
    "@slack/bolt": "^3.21.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
